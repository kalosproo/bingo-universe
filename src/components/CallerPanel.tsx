import { FaBullhorn } from 'react-icons/fa6';
export function CallerPanel({called,onCall}:{called:number[];onCall:(n:number)=>void}){ const remaining=Array.from({length:75},(_,i)=>i+1).filter(n=>!called.includes(n)); const next=()=>{if(remaining.length) onCall(remaining[Math.floor(Math.random()*remaining.length)])}; return <section className="rounded-2xl bg-white/10 p-4"><button className="rounded bg-fuchsia-500 px-4 py-2 font-bold" onClick={next}><FaBullhorn className="inline"/> Call number</button><p className="mt-3">Last: {called.at(-1) ?? '—'}</p><p>Called: {called.join(', ') || 'None yet'}</p></section>}
